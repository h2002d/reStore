@model List<LaserArt.Models.Product>
@{
    ViewBag.Title = "Order";
}
<input type="hidden" value="@ViewBag.ListInt" id="listInt" />
<div class="jumbotron">
    
    @foreach (var item in Model)
    {
        <div class="row orderItem">
        <div class="row">
            <div class="col-md-3">
                <p>Название</p>
            </div>
            <div class="col-md-6">
                <p>Описание</p>
            </div>
            <div class="col-md-3">
                <p>Цена</p>
            </div>
        </div>
            <hr class="productPagehr"/>
        <div class="row ">
            <div class="col-md-3">
                <p>@item.ProductTitle</p>
                <img src="@item.ImageSource" class="imageHeader" />
            </div>
            <div class="col-md-6">
                <p>@item.ProductDescription</p>
            </div>
            <div class="col-md-3">
                <p>@item.Price$</p>
                @if (@item.PriceDiscounted != 0)
                {
                    <del> @item.PriceDiscounted</del>
                }
            </div>
        </div>
    </div>
        <hr />
    }
    <div class="row" align="right">
        <p>Сумма заказа:@ViewBag.Sum</p>
        <input type="button" value="Заказать" onclick="goToDetails()"/>
    </div>
</div>

